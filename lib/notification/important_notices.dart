import 'package:flutter/material.dart';

class ImportantNoticesTab extends StatelessWidget {
  const ImportantNoticesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('重要なお知らせの内容'),
    );
  }
}
