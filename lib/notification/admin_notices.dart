import 'package:flutter/material.dart';

class AdminNoticesTab extends StatelessWidget {
  const AdminNoticesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('運営からのお知らせの内容'),
    );
  }
}
