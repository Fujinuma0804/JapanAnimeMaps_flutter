import 'package:flutter/material.dart';

class ContactUsTab extends StatelessWidget {
  const ContactUsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('お問い合わせの内容'),
    );
  }
}
